if SHIELD_FORAGER_PLUS_LEFT

config ZMK_KEYBOARD_NAME
    default "Forager+"

config ZMK_SPLIT_ROLE_CENTRAL
    default y

config ZMK_STUDIO
    default y

endif

if SHIELD_FORAGER_PLUS_LEFT || SHIELD_FORAGER_PLUS_RIGHT

config ZMK_SPLIT
    default y

endif
